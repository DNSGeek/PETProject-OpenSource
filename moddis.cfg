MEMORY {
    LOADADDR: start = $A000, size = $02,   file = %O;
    MAIN:     start = $A000, size = $1800, file = %O, define = yes;
}
SEGMENTS {
    LOADADDR: load = LOADADDR, type = ro;
    CODE:     load = MAIN,     type = ro;
}
