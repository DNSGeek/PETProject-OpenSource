# Linker config for PETProject modules
# All modules load at $C000.
# The 2-byte PRG header ($00 $C0) is written by the LOADADDR segment.
# Code starts immediately after at $C000.

# No ZP memory area: modules take zero-page scratch from zp.inc (a fixed,
# collision-checked pool), never from a linker-placed .zeropage segment —
# the editor's own .zeropage block is live at $02 while a module runs.
MEMORY {
    LOADADDR: start = $0000, size = $0002, file = %O;
    MAIN:     start = $C000, size = $0FFE, file = %O, define = yes;
}

SEGMENTS {
    LOADADDR: load = LOADADDR, type = ro;
    CODE:     load = MAIN,     type = ro,  define = yes;
    BSS:      load = MAIN,     type = bss, define = yes, optional = yes;
}
